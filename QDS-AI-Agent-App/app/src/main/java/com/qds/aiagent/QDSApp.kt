package com.qds.aiagent

import android.app.Application

class QDSApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize app-wide settings if needed
    }
}
